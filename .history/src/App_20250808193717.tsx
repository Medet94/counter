import axios from 'axios';
import {
  baseURL,
  connection,
  connectionSearch,
  connectorTypes,
  connectionById,
} from './url';

export const App = () => {
  console.log(`${baseURL}, ${connection}`);

  return <></>;
};
